# Database Configuration
# For docker-compose, use: postgres://authuser:CHANGE_ME@postgres:5432/auth_db
DATABASE_URL=postgres://authuser:CHANGE_ME_IN_PRODUCTION@localhost:5432/auth_db
DATABASE_MAX_CONNECTIONS=5

# JWT Configuration
# IMPORTANT: Use a strong random string (minimum 32 characters) in production
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_IN_PRODUCTION_MIN_32_CHARS
JWT_ACCESS_TOKEN_MINUTES=15
JWT_REFRESH_TOKEN_DAYS=7
