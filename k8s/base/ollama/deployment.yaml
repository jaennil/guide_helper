apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  labels:
    app: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      initContainers:
      - name: pull-model
        image: ghcr.io/jaennil/ollama:latest
        command:
        - sh
        - -c
        - |
          ollama serve &
          OLLAMA_PID=$!
          sleep 5
          ollama pull $(OLLAMA_MODEL)
          if [ "$(OLLAMA_VISION_MODEL)" != "$(OLLAMA_MODEL)" ]; then
            ollama pull $(OLLAMA_VISION_MODEL)
          fi
          kill $OLLAMA_PID 2>/dev/null || true
        env:
        - name: OLLAMA_MODEL
          valueFrom:
            configMapKeyRef:
              name: ollama-config
              key: OLLAMA_MODEL
        - name: OLLAMA_VISION_MODEL
          valueFrom:
            configMapKeyRef:
              name: ollama-config
              key: OLLAMA_VISION_MODEL
        volumeMounts:
        - name: ollama-data
          mountPath: /root/.ollama
        resources:
          requests:
            memory: "2Gi"
            cpu: "500m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
      containers:
      - name: ollama
        image: ghcr.io/jaennil/ollama:latest
        ports:
        - containerPort: 11434
          name: http
        livenessProbe:
          httpGet:
            path: /api/tags
            port: 11434
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/tags
            port: 11434
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            memory: "2Gi"
            cpu: "500m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
        volumeMounts:
        - name: ollama-data
          mountPath: /root/.ollama
      volumes:
      - name: ollama-data
        persistentVolumeClaim:
          claimName: ollama-pvc
