# C4-диаграммы системы Guide Helper

Дата: 21 апреля 2026  
Назначение: показать систему в формате C4 на уровнях Context, Container и Component.

## 1. Как использовать эти диаграммы на защите

Если надо объяснить проект быстро:
- сначала показывается `C1`, чтобы объяснить, кто взаимодействует с системой;
- потом `C2`, чтобы объяснить, из каких контейнеров состоит решение;
- потом `C3`, чтобы показать внутреннее устройство самого важного контейнера, то есть `Routes Service`.

## 2. C1. System Context

```mermaid
flowchart LR
    Guide[Гид / Автор маршрута]
    Tourist[Турист]
    Admin[Администратор]

    GH[Guide Helper]

    Nominatim[Nominatim]
    AI[AI providers]
    Maps[Map tile source]

    Guide -->|создаёт, редактирует, публикует маршруты| GH
    Tourist -->|просматривает, оценивает, комментирует, сохраняет| GH
    Admin -->|контролирует категории, статистику, контент| GH

    GH -->|геокодинг| Nominatim
    GH -->|генерация текста и AI-чат| AI
    GH -->|картографические тайлы| Maps
```

### Что говорить

Guide Helper находится между тремя основными ролями пользователя и тремя ключевыми внешними интеграциями:
- геокодинг;
- AI;
- картографические тайлы.

## 3. C2. Container Diagram

```mermaid
flowchart LR
    subgraph Clients[Клиенты]
        Web[Web PWA]
        Desktop[Desktop shell on Tauri]
        Mobile[Android companion app]
    end

    subgraph Backend[Backend containers]
        Auth[Auth Service]
        Routes[Routes Service]
        Worker[Photo Worker]
        Tiles[Tiles Service]
        Cache[Cache Service]
    end

    subgraph Data[Data / Messaging]
        Postgres[(PostgreSQL)]
        MinIO[(MinIO)]
        NATS[(NATS JetStream)]
        Redis[(Redis)]
    end

    subgraph External[External systems]
        Geo[Nominatim]
        AI[claude-code-api / OpenAI / Ollama / Anthropic]
        OSM[Tile source]
    end

    Web --> Auth
    Web --> Routes
    Web --> Tiles

    Desktop --> Auth
    Desktop --> Routes
    Desktop --> Tiles

    Mobile --> Auth
    Mobile --> Routes
    Mobile --> Tiles

    Auth --> Postgres
    Routes --> Postgres
    Routes --> NATS
    Routes --> MinIO
    Routes --> Geo
    Routes --> AI

    Worker --> NATS
    Worker --> Postgres
    Worker --> MinIO

    Tiles --> Cache
    Cache --> Redis
    Cache --> OSM
```

### Контейнеры и их роль

| Контейнер | Роль |
| --- | --- |
| Web PWA | основной пользовательский интерфейс |
| Desktop shell | desktop-вариант web-клиента |
| Android app | полевой сбор маршрута |
| Auth Service | аутентификация, роли, JWT |
| Routes Service | основной доменный сервис |
| Photo Worker | фоновая обработка фотографий |
| Tiles Service | выдача тайлов карт |
| Cache Service | кеш тайлов |
| PostgreSQL | основная БД |
| MinIO | объектное хранилище фото |
| NATS JetStream | событийная шина |
| Redis | быстрый кеш |

## 4. C3. Component Diagram для Routes Service

```mermaid
flowchart LR
    HTTP[HTTP API / Axum handlers]
    WS[WebSocket / SSE endpoints]

    RoutesUC[RoutesUseCase]
    CommentsUC[CommentsUseCase]
    LikesUC[LikesUseCase]
    RatingsUC[RatingsUseCase]
    BookmarksUC[BookmarksUseCase]
    CategoriesUC[CategoriesUseCase]
    NotificationsUC[NotificationsUseCase]
    ChatUC[ChatUseCase]
    SettingsUC[SettingsUseCase]

    Repo[Postgres repositories]
    Geo[Nominatim client]
    AI[Dynamic AI client]
    Events[NATS publisher/subscriber]
    Storage[MinIO / photo URLs]

    HTTP --> RoutesUC
    HTTP --> CommentsUC
    HTTP --> LikesUC
    HTTP --> RatingsUC
    HTTP --> BookmarksUC
    HTTP --> CategoriesUC
    HTTP --> NotificationsUC
    HTTP --> ChatUC
    HTTP --> SettingsUC

    WS --> ChatUC
    WS --> NotificationsUC

    RoutesUC --> Repo
    RoutesUC --> Geo
    RoutesUC --> Events
    RoutesUC --> Storage
    RoutesUC --> AI

    CommentsUC --> Repo
    LikesUC --> Repo
    RatingsUC --> Repo
    BookmarksUC --> Repo
    CategoriesUC --> Repo
    NotificationsUC --> Repo
    ChatUC --> Repo
    ChatUC --> AI
    SettingsUC --> Repo
```

### Что важно объяснить

`Routes Service` это не просто CRUD маршрутов. Он содержит:
- доменную логику маршрутов и версий;
- social layer;
- AI-чат;
- геокодинг;
- связку с очередью обработки фотографий;
- уведомления;
- настройки сложности маршрута.

Поэтому именно этот контейнер и является сердцем системы.

## 5. C3. Component Diagram для Frontend

```mermaid
flowchart LR
    Router[React Router]

    MapPage[MapPage]
    ExplorePage[ExplorePage]
    ProfilePage[ProfilePage]
    AdminPage[AdminPage]
    SharedPage[SharedMapPage]
    BookmarksPage[BookmarksPage]

    GeoSearch[GeoSearchControl]
    ChatPanel[ChatPanel]
    Weather[WeatherPanel]
    Timeline[HistoricalMapOverlay]
    Stats[RouteStatsPanel]
    Comments[CommentSection]
    Social[LikeRatingBar]
    Notifications[NotificationBell]

    Api[REST / WS API clients]

    Router --> MapPage
    Router --> ExplorePage
    Router --> ProfilePage
    Router --> AdminPage
    Router --> SharedPage
    Router --> BookmarksPage

    MapPage --> GeoSearch
    MapPage --> ChatPanel
    MapPage --> Weather
    MapPage --> Timeline
    MapPage --> Stats
    MapPage --> Comments
    MapPage --> Social
    MapPage --> Notifications

    ExplorePage --> Api
    ProfilePage --> Api
    AdminPage --> Api
    SharedPage --> Api
    BookmarksPage --> Api
    MapPage --> Api
```

### Что говорить

Frontend это не одна карта, а целый набор специализированных страниц:
- редактор маршрута;
- каталог;
- профиль;
- закладки;
- админка;
- общий просмотр по ссылке.

## 6. C4-картина в одном абзаце

На уровне контекста Guide Helper взаимодействует с гидом, туристом и администратором, а также с геокодингом, AI и картографическими сервисами. На уровне контейнеров система состоит из web и mobile клиентов, набора backend-сервисов, очереди сообщений, БД и объектного хранилища. На уровне компонентов ядром решения является `Routes Service`, который объединяет маршруты, социальные функции, AI, уведомления и обработку событий.

## 7. Что можно отдельно дорисовать позже

Если понадобится усилить защиту ещё сильнее, можно добавить:
- sequence diagram “загрузка фото и его асинхронная обработка”;
- sequence diagram “запрос AI-чата с map context”;
- deployment diagram Kubernetes.
